// Ancho máximo del contenido en pantalla, centrado. En un celular real (~350-430px)
// nunca se llega a este límite, así que no cambia nada ahí; solo evita que la UI se
// estire a lo ancho de una ventana de escritorio cuando se previsualiza con Expo Web.
export const MAX_CONTENT_WIDTH = 480;
